#include "main.h"
/**
 * _isupper - check for uppercase character
 * @c: is the chaacter to be checked
 * Return: 1 if c is upper and 0 if not upper
 */
int _isupper(int c)
{

	return (c >= 'A' && c <= 'Z');
}
