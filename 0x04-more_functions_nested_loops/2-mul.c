#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: int to be mutipled with b
 * @b: int to be mutipled with a
 * Return: The result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
